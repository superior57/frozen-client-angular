import { ArrayChangesDetectorPipe } from './array-changes-detector.pipe';

describe('ArrayChangesDetectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayChangesDetectorPipe();
    expect(pipe).toBeTruthy();
  });
});
