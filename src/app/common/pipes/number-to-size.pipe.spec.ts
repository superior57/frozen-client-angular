import { NumberToSizePipe } from './number-to-size.pipe';

describe('NumberToSizePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToSizePipe();
    expect(pipe).toBeTruthy();
  });
});
